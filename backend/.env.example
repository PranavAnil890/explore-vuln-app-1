# FIX: no real secret checked in - generate your own strong random value, e.g.:
#   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
# Must be at least 32 characters or the server will refuse to start.
JWT_SECRET=replace_this_with_a_long_random_generated_secret

MONGO_URI=mongodb://localhost:27017/exploreApp
PORT=5000

# FIX: explicit CORS allowlist, comma-separated for multiple frontends
ALLOWED_ORIGINS=http://localhost:3000
